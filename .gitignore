node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test
!.env.staging.example
!.env.production.example

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
.gstack/

.env.oauth
.env*

# Supabase CLI local state (project link cache, no secrets but not versioned)
supabase/.temp/

# Salida intermedia y reproducible de scripts/data/generate-municipios.py
# (se regenera desde el fichero oficial del INE; su contenido ya vive
# íntegro en la migración correspondiente, no hace falta versionarla).
scripts/data/output/
