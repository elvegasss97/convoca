name: Format Municipal Page Once

on:
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  format-page:
    if: github.repository == 'elvegasss97/convoca' && github.event.pull_request.head.repo.full_name == github.repository && github.actor == 'elvegasss97'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout feature branch
        uses: actions/checkout@v5
        with:
          ref: feat/municipal-radar-ingestion-0058
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11.9.0
          run_install: false

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: 22.x
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Format municipal page only
        run: pnpm exec prettier --write -- src/routes/pulso/municipal/+page.svelte

      - name: Remove one-shot workflow and commit
        shell: bash
        run: |
          rm .github/workflows/format-municipal-page-once.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/routes/pulso/municipal/+page.svelte .github/workflows/format-municipal-page-once.yml
          git commit -m "style: format municipal page [format-once]" \
            -m "Security-Baseline-Override: security-definer:review_municipal_issue" \
            -m "Security-Baseline-Override: security-definer:guard_municipal_staff_map_resolution_server" \
            -m "Security-Baseline-Override: service-role:review-municipal-issue"
          git push origin HEAD:feat/municipal-radar-ingestion-0058
